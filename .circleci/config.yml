version: 2.1

executors:
  default:
    docker:
      - image: circleci/python:3.8
    working_directory: ~/workspace

jobs:
  deploy-vpc-1:
    executor: default
    steps:
      - checkout
      - run:
          name: Install AWS CLI
          command: |
            curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
            unzip awscliv2.zip
            sudo ./aws/install
      - run:
          name: Deploy First VPC using script
          command: |
            chmod +x ./scripts/deply.sh
            ./scripts/deply.sh templates/VPC.yml VPC my-vpc-stack-1

  deploy-vpc-2:
    executor: default
    steps:
      - checkout
      - run:
          name: Install AWS CLI
          command: |
            curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
            unzip awscliv2.zip
            sudo ./aws/install
      - run:
          name: Deploy Second VPC using script
          command: |
            chmod +x ./scripts/deply.sh
            ./scripts/deply.sh templates/VPC2.yml VPC2 my-vpc-stack-2


  deploy-peering:
    executor: default
    steps:
      - checkout
      - run:
          name: Install AWS CLI
          command: |
            curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
            unzip awscliv2.zip
            sudo ./aws/install
      - run:
          name: Deploy VPC Peering using script
          command: |
            chmod +x ./scripts/deply.sh
            ./scripts/deply.sh templates/Peering.yml peering deploy-peering

workflows:
  version: 2
  vpc-deploy:
    jobs:
      - deploy-vpc-1:
          requires:
            - deploy-vpc-2
          context: 
            - region
            - idaws
            - clavaws
      - deploy-vpc-2:
          context: 
            - region
            - idaws
            - clavaws
      - deploy-peering:
          requires:
            - deploy-vpc-1
            - deploy-vpc-2
          context: 
            - region
            - idaws
            - clavaws
